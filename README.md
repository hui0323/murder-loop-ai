# murder-loop-ai

《murder-loop-ai》是一款多 AI 协作驱动的悬疑时间循环互动小说游戏。玩家通过自然语言输入行动，系统用规则引擎维护世界事实，由行动解析 AI、凶手 AI、叙事 AI、NPC 模块和评分系统共同推动一场发生在 23:47 前后的悬疑生存循环。

项目当前处于 MVP 原型阶段：核心文字循环、服务端 AI 接口、前端文本交互界面、本地 fallback 规则与基础评分已经搭建完成，但完整剧情分支、视觉资产、持久化存档和更复杂的证据链仍在开发中。

---

## 1. 项目背景

很多 AI 剧情产品采用“玩家输入 → 单个 AI 续写剧情”的模式。这种方式实现简单，但容易出现几个问题：

- AI 胡编事实，破坏前后逻辑；
- AI 过度顺从玩家，缺少真正对抗；
- 一个 AI 同时扮演作者、裁判、凶手和 NPC，容易人格混乱；
- AI 知道所有信息，角色经常开上帝视角；
- 没有规则系统约束，玩家死亡或通关缺乏可信原因。

本项目尝试把 AI 从“万能作者”拆分为多个受约束的协作角色：

```txt
玩家自然语言输入
→ 行动解析 AI
→ 本地规则裁判系统
→ 凶手 AI 在有限信息下反制
→ 规则系统校验凶手策略
→ 叙事 AI 小说化输出
→ 证据、记忆、评分系统记录结果
```

核心原则是：

```txt
玩家和 AI 共同创作故事，
但规则系统拥有最终解释权。
```

---

## 2. 游戏内容概述

### 一句话定义

你不是在阅读悬疑小说，而是在和 AI 一起写一部会杀死你的小说。

### 核心设定

故事发生在青荷公寓 503 室。主角沈知夏刚搬进这间出租屋，却因为一个被送错的包裹卷入危险。这个包裹原本属于房东陈怀民控制的地下转运链，里面隐藏着能够暴露其犯罪行为的关键线索。

第一轮中，沈知夏没有意识到危险，在 23:47 被杀。死亡后，她带着模糊的死亡碎片回到 23:00，必须利用每一轮获得的记忆，逐步理解包裹、房东、林越、假警察和 23:47 的意义。

### 核心人物

- **沈知夏**：玩家扮演的主角，刚搬入青荷公寓 503。
- **陈怀民**：青荷公寓房东，真实身份是地下转运链的关键人物，也是凶手。
- **林越**：沈知夏的前男友，不是凶手，但可能成为救援变量、误导源或风险点。
- **真警察 / 假警察**：报警不是自动通关，玩家需要核实身份并保全证据。

### 玩家目标

活下来只是最低目标。更好的结局需要玩家：

- 保住自己；
- 保留包裹、照片、录音等证据；
- 让林越和无辜 NPC 尽量安全；
- 识别假警察与陈怀民的谎言；
- 理解 23:47 与包裹转运的真相；
- 让警方获得可信证据，而不是被嫁祸或误导。

---

## 3. 核心玩法

玩家主要通过自然语言输入行动，而不是点击固定选项。

示例：

```txt
我不直接拆包裹，先拍一张照片发给林越，然后把包裹恢复原样，再用椅子抵住门。
```

系统会将输入拆分为结构化行动，例如：保存证据、联系林越、加固门、推进时间、增加噪音风险等。随后规则系统判断行动是否可行，并更新世界状态。

### 单回合流程

```txt
玩家输入行动
→ AI 解析行动
→ 规则系统执行玩家行动
→ 凶手 AI 根据已知信息选择策略
→ 规则系统校验凶手行动
→ 叙事 AI 输出小说化反馈
→ 更新时间、威胁、线索、NPC 和结局状态
```

### 凶手对抗原则

凶手 AI 不能读取完整游戏状态，只能看到“凶手可能知道的信息”。例如，玩家如果把包裹藏进卫生间水箱，但没有发出明显声音、没有暴露位置，凶手不能凭空知道证据位置。

凶手可能采取的策略包括：

- 短信试探；
- 轻敲门；
- 假装房东维修；
- 假警察施压；
- 备用钥匙开门；
- 断电；
- 窗户路线；
- 引诱林越上楼；
- 退避、嫁祸或转移证据。

---

## 4. 当前技术架构

项目采用 Monorepo 结构。

```txt
murder-loop-ai/
├─ apps/
│  ├─ web/              # React + Vite 前端
│  └─ server/           # Fastify AI 服务端
│
├─ packages/
│  ├─ shared/           # 共享类型、时间工具
│  ├─ ai-contracts/     # AI 输出 Zod schema
│  ├─ content/          # 故事、房间、线索、NPC 内容
│  └─ game-core/        # 本地规则系统、凶手策略、评分、循环逻辑
│
├─ docs/                # 设计文档与 MVP 清单
├─ package.json
└─ README.md
```

### 前端

- React
- TypeScript
- Vite
- Zustand / 本地状态管理方向
- Framer Motion / motion 动效
- 文本剧情面板
- 玩家输入框
- 右侧线索与状态栏
- 开场与结局过场动画

### 后端

- Node.js
- Fastify
- TypeScript
- OpenAI-compatible SDK 调用方式
- 多 provider / 多 role AI 配置
- Zod schema 校验 AI 输出
- 本地 fallback 保证无 API key 时仍能跑通核心闭环

### 核心包

- `packages/shared`：定义 GameState、ActionPlan、KillerStrategy、Narration、ScoreResult 等核心类型。
- `packages/ai-contracts`：约束 AI 输出结构，防止模型返回不可用内容。
- `packages/content`：维护故事圣经、房间信息、线索和 NPC 内容。
- `packages/game-core`：实现行动执行、凶手策略应用、回溯、叙事上下文、评分等规则逻辑。

---

## 5. 已实现内容

根据当前代码和 MVP 清单，项目已经完成以下基础能力：

### 项目基础

- [x] Monorepo 基础结构
- [x] Web 前端应用
- [x] Fastify 服务端
- [x] 共享类型系统
- [x] AI 输出 schema
- [x] 中文故事、房间、线索内容

### 游戏核心

- [x] 初始游戏状态
- [x] 23:00 到 23:47 的时间推进
- [x] 玩家自然语言输入
- [x] fallback 行动解析
- [x] 本地规则裁判
- [x] 线索添加与状态更新
- [x] 凶手知识边界投影
- [x] fallback 凶手策略
- [x] AI 凶手策略接口
- [x] AI 叙事接口
- [x] 叙事 fallback
- [x] 基础评分 fallback
- [x] 死亡 / 生还结局字段

### 已支持的行动方向

- 检查包裹、门锁、窗户、房间；
- 拍照、备份、录音；
- 联系林越；
- 联系房东并试探；
- 报警；
- 核实警察身份；
- 加固门窗；
- 隐藏证据；
- 开门；
- 等待观察；
- 自我调整。

### 已支持的部分结局 / 风险路线

- 23:47 默认死亡；
- 假警察开门死亡；
- 窗户路线死亡；
- 带证据生还；
- 低证据或错误行动导致的低分结果框架。

### 服务端接口

当前服务端包含：

```txt
GET  /health
POST /api/parse-action
POST /api/killer-strategy
POST /api/narrate
POST /api/narrate-action
POST /api/narrate-ambient
POST /api/npc-reply
POST /api/score-run
POST /api/frontend/resolve-action
```

其中 `/api/frontend/resolve-action` 是当前前端主流程使用的聚合接口，会完成行动解析、规则执行、凶手策略、叙事输出和前端数据适配。

---

## 6. AI 配置说明

项目把 AI 配置分成两层：

1. **Provider 级配置**：配置某个模型服务的 key、base URL 和默认模型。
2. **Role 级配置**：配置某个游戏角色或功能使用哪个 provider。

### Provider 示例

```env
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL=gpt-4o-mini

DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

DEEPSEEK_KILLER_API_KEY=
DEEPSEEK_KILLER_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_KILLER_MODEL=deepseek-v4pro

DEEPSEEK_NPC_API_KEY=
DEEPSEEK_NPC_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_NPC_MODEL=deepseek-v4pro

DEEPSEEK_RECAP_API_KEY=
DEEPSEEK_RECAP_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_RECAP_MODEL=deepseek-v4pro

DUCKINGMIND_API_KEY=
DUCKINGMIND_BASE_URL=https://api.duckingmind.com/v1
DUCKINGMIND_MODEL=gpt-4o-mini
```

### Role 示例

```env
AI_PARSE_PROVIDER=deepseek
AI_KILLER_PROVIDER=deepseek_killer
AI_NARRATOR_PROVIDER=duckingmind
AI_NPC_PROVIDER=deepseek_npc
AI_RECAP_PROVIDER=deepseek_recap

AI_PARSE_MODEL=
AI_KILLER_MODEL=deepseek-v4pro
AI_NARRATOR_MODEL=
AI_NPC_MODEL=
AI_RECAP_MODEL=
```

说明：即使没有配置 API key，项目也会使用本地 fallback 跑通核心游戏循环。

---

## 7. 开发命令

安装依赖：

```bash
npm install
```

启动前端和服务端：

```bash
npm run dev
```

分别启动：

```bash
npm run dev:web
npm run dev:server
```

类型检查：

```bash
npm run typecheck
```

构建：

```bash
npm run build
```

默认服务端地址：

```txt
http://127.0.0.1:8788
```

前端通过 Vite 代理访问 `/api`。

---

## 8. 当前开发进度评估

### 已完成阶段

#### Phase 0：设计确认

基本完成。已有完整软件设计文档、MVP 清单、故事设定和核心玩法定义。

#### Phase 1：项目初始化

基本完成。Monorepo、前端、服务端、共享类型、AI contracts、game-core 都已经建立。

#### Phase 2：核心文字循环

已完成原型闭环。玩家可以输入行动，服务端执行解析、规则裁判、凶手策略和叙事输出，前端显示结果。

### 进行中阶段

#### Phase 3：凶手对抗系统

已经具备基础版本：凶手知识边界、fallback 策略、AI 策略接口、假警察/备用钥匙/窗户路线等核心路线已存在。但凶手长期规划、动态欺骗、多轮记忆利用和更复杂的嫁祸逻辑仍需要增强。

#### Phase 4：证据与评分

已有基础评分函数和线索系统，但证据链仍偏简化。当前更多是 MVP 级别的“线索 ID + 状态变化”，还未形成完整的证据推理、证据污染、证据提交和警方可信度系统。

### 尚未充分展开阶段

#### Phase 5：视觉与氛围

已有前端暗色悬疑 UI、开场/结局过场和基础动效，但尚未完成规划中的户型图、关键分镜、音效、出租屋主视觉和证据可视化。

---

## 9. 当前缺陷与限制

### 玩法层面

- 行动解析仍依赖 schema 和 fallback，复杂中文长指令可能解析不完整。
- 玩家确认系统还不完整，当前前端主要是直接提交并执行。
- 玩家自由度很高，但规则覆盖面还有限，很多细节行动会退化为默认处理。
- 23:47 后的升级阶段尚未充分展开。
- 多轮回溯记忆机制还没有完全产品化。
- 部分结局字段已经定义，但实际触发路径还不够丰富。

### 凶手 AI 层面

- 凶手策略已有有限信息原则，但长期计划能力仍偏弱。
- 凶手对玩家行为的识别主要依赖规则状态，缺少更细颗粒度的观察、误判和推理过程。
- 嫁祸、撤离、转移证据等高阶策略还需要扩展。
- 当前凶手压力更像“单步策略选择”，还不是完整的持续对抗状态机。

### 叙事层面

- 叙事 AI 已接入行动叙事和环境叙事，但风格稳定性仍依赖 prompt 和导演评分器。
- fallback 叙事能保证可玩，但文学表现力有限。
- 叙事与线索展示之间还可以更紧密，避免玩家不知道哪些文本是有效信息。

### 技术层面

- 尚未接入数据库，缺少持久化存档、路线记录和 AI 决策日志。
- `.env` 为本地配置文件，不能提交真实 key；需要通过 `.env.example` 维护示例。
- 测试覆盖不足，规则系统和 AI 输出校验需要更多自动化测试。
- 前端 `App.tsx` 中仍保留部分 mock confirmation 逻辑，需要清理或接入真实确认流程。
- 当前 health 只粗略显示 provider 是否有 key，没有完整验证模型调用可用性。
- 项目中部分设计文档提到 SQLite，但当前还未实际接入。

---

## 10. 未来计划

### 短期计划

- 完善玩家行动确认流程：先展示 AI 理解，再允许玩家确认、修改或取消。
- 增强行动解析：支持多动作拆分、条件句、否定词、计划与真实行动区分。
- 扩展规则系统：覆盖更多房间物品、门窗状态、手机状态和包裹处理方式。
- 完善凶手策略：强化假警察、备用钥匙、断电、窗户路线、引诱出门等路线。
- 增强 23:47 后阶段：灭证、嫁祸、撤离、真警察介入、升级暴力。
- 清理前端 mock 逻辑，使前端完全围绕真实后端结果运行。

### 中期计划

- 建立完整证据链系统：照片、录音、聊天记录、门锁痕迹、快递信息、官方回拨记录。
- 接入 SQLite 或其他本地数据库，保存玩家路线、结局评分和 AI 决策日志。
- 完成多轮回溯记忆：死亡碎片、已知真相、错误教训、保留记忆展示。
- 扩展 NPC 系统：林越、真警察、假警察、邻居、房东之间的动态关系。
- 增加更多结局：被嫁祸的幸存者、低分逃脱、高分反制、完美真相结局。
- 增加自动化测试，尤其是规则系统、结局触发和 AI schema 校验。

### 长期计划

- 完成视觉与氛围层：户型图、关键图片、猫眼视角、死亡碎片、音效、UI 动效。
- 做成可重复游玩的悬疑沙盒：不同路线、不同信息暴露、不同凶手反制。
- 建立 AI 导演系统，动态调节节奏、压力和叙事质量。
- 支持更完整的复盘报告：解释玩家为什么死、哪里做对、哪里做错、下一轮可如何改进。
- 打磨成完整 Demo 或可发布的 Web 互动小说游戏。

---

## 11. 手工验证路线

可以用以下路线测试当前 MVP：

1. 输入：

```txt
我先不拆包裹，拍照发给林越，然后把椅子抵住门。
```

检查：时间推进、线索增加、林越状态变化、门防御状态变化。

2. 输入：

```txt
门外说自己是警察，我直接开门。
```

检查：是否触发高风险或死亡。

3. 尝试更稳妥路线：

```txt
我不开门，打开录音，要求对方报单位和警号，然后拨打 110 回拨核实。
```

检查：是否增加警察核验、录音等线索，并降低假警察风险。

4. 尝试证据生还路线：

```txt
我把包裹拍照备份到云端，发给林越，让他在楼下报警，不要上楼，然后锁窗拉帘并用行李箱堵门。
```

检查：是否更容易进入带证据生还或高分路线。

---

## 12. 项目定位总结

《murder-loop-ai》不是传统图形化逃脱游戏，也不是单 AI 聊天续写工具。它的核心体验是：

- 玩家用语言行动；
- 规则系统决定事实；
- 凶手 AI 在有限信息下反制；
- 叙事 AI 把结果写成悬疑小说；
- 每次死亡都变成下一轮的线索；
- 最终目标不是简单活下来，而是在公平规则下理解真相、保存证据并反制凶手。

项目当前已经具备 MVP 闭环，接下来的重点是扩大规则覆盖面、强化凶手对抗、完善证据链与回溯记忆，并将前端体验从原型打磨成完整可玩的悬疑互动小说 Demo。
